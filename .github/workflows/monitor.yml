name: Posio Kanalintulupa Monitor

"on":
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  check-permits:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Lataa koodi
        uses: actions/checkout@v4

      - name: Tarkista lupasaatavuus
        run: python3 check_permits.py
